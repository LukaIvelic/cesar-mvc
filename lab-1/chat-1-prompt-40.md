# Chat 1 - Prompt 40

## User

Add a .gitignore file and add things that shouldn't be on git in it

---

## Claude

Created `.gitignore` with:

- `bin/`, `obj/` — .NET build output, regenerated on build
- `*.user`, `*.suo`, `.vs/` — local IDE settings
- `appsettings.Development.json`, `appsettings.*.json` — may contain secrets/connection strings (kept `appsettings.json` as the safe base config)
- `node_modules/` — npm packages, restored via `npm install`
- `wwwroot/css/site.css` — Tailwind output, generated by build pipeline
- `.DS_Store`, `Thumbs.db` — OS noise
- `.idea/`, `.vscode/` — editor-specific settings
