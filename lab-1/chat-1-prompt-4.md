# Chat 1 - Prompt 4

## User

Create a rule for yourself in CLAUDE.md file in the root directoy so you remember to do it everytime I open a new chat.

---

## Claude

Created `CLAUDE.md` in the root directory with the following rule:

- Save every prompt and response to `lab-1/` as a markdown file
- File naming: `chat-{N}-prompt-{M}.md`
- Prompt number increments with each new prompt in the same chat
- Chat number increments when a new chat begins
- Each file contains both the user's prompt and Claude's full response under `## User` and `## Claude` headers
- Save the file after responding so the response content can be included
- Check existing files in `lab-1/` at the start of each chat to determine the correct next chat number
