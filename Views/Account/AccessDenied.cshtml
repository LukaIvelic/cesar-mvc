@{
    ViewData["Title"] = "Access denied";
}

<div class="page-shell page-shell-narrow">
    <div class="detail-panel">
        <span class="badge">Permission required</span>
        <h2 class="page-title mt-4">Access denied</h2>
        <p class="page-subtitle">
            Your account is signed in, but it does not have permission to open this action.
        </p>
        <div class="mt-5">
            <a asp-controller="Home" asp-action="Index" class="btn-secondary">Return home</a>
        </div>
    </div>
</div>
